/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
        domains: ['encrypted-tbn0.gstatic.com', 'miro.medium.com'], // Add the hostname of the image source here
    },
}
